<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.assessment.demos.web.mapper.ProductMapper">
    <update id="reduceStock">
        UPDATE product SET current_inventory = current_inventory - #{quantity}, update_time = #{updateTime} WHERE id = #{productId}
    </update>
    <update id="importUpdateProduct">
        UPDATE product
        SET current_inventory = current_inventory + #{quantity},
            unit_price = #{unitPrice},
            update_time = #{updateTime}
        WHERE id = #{productId}
    </update>

    <select id="list" resultType="com.example.assessment.demos.web.entity.Product">
        SELECT * FROM product
        <where>
            <if test="productName != null">
                product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
        </where>
    </select>
</mapper>