<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.assessment.demos.web.mapper.OrderMapper">
    <insert id="addOrder">
        INSERT INTO sys_order (customer_id, product_id, quantity, status, amount, order_number, file)
        VALUES (#{customerId}, #{productId}, #{quantity}, #{status}, #{amount}, #{orderNumber}, #{filePath})
    </insert>

    <select id="orderQuery" resultType="com.example.assessment.demos.web.entity.SysOrder">
        SELECT o.* FROM sys_order o join customer c on o.customer_id = c.id
        <where>
            c.salesman_id = #{userId}
            <if test="status != null">
                and o.status = #{status}
            </if>
            <if test="beginTime != null">
                and o.create_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and o.create_time &lt;= #{endTime}
            </if>
        </where>
        order by o.create_time desc
    </select>

    <select id="orderQueryAll" resultType="com.example.assessment.demos.web.entity.SysOrder">
        SELECT o.* FROM sys_order o join customer c on o.customer_id = c.id
        <where>
            <if test="status != null">
                and o.status = #{status}
            </if>
            <if test="beginTime != null">
                and o.create_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and o.create_time &lt;= #{endTime}
            </if>
        </where>
        order by o.create_time desc
    </select>
</mapper>